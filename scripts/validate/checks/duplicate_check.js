// Duplicate check is handled in main for performance, so this is a placeholder for future modular use.
export function duplicateCheck(data, seenIds) {
  // not used directly in main yet
  return null;
}
